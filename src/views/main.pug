doctype html
html
    head
        meta(charset="utf-8")
        title Library
        link(rel='stylesheet', href='/src/style/main.css')
    body
        header
            .header_name Библиотека
        section
            ul
                each book in books
                    li
                        a 
                            =`${book.id}: ${book.title} (${book.author})`