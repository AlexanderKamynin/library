doctype html
html
    head
        meta(charset="utf-8")
        title Book delete
        link(rel='stylesheet', href='/src/style/delete.css')
    body
        form(method="get", action=`/books/`)
            button(type="submit")
                =`Вернуться`

        form(method="post", action=`/books/delete_action`)
            div
                p 
                    ="Введите id удаляемой книги"
                input(type="text", name="delete_id")

            div 
                input(type="submit", value="Удалить книгу")