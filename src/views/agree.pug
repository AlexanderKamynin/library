doctype html
html
    head
        meta(charset="utf-8")
        title Book delete
        link(rel='stylesheet', href='/src/style/agree.css')
    body
        dialog(open)
            form(method="post", action=`/books/delete_action/agree`)
                div
                    p 
                        =`Вы действительно хотите удалить книгу с id=${book_id}?`

                div 
                    input(type="submit", value="Да", name="agree")
                div 
                    input(type="submit", value="Нет", name="agree")